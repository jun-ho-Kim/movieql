type Movie {
    id: Int!,
    title: String!,
    rating: Float,
    year: Int,
    summary: String!
}

type Query {
    movies(limit: Int, rating:Float): [Movie]!
    movie(id: Int!): Movie
}

type Mutation {
    addMovie(name: String! score: Int!): Movie!
    deleteMovie(id: Int!) : Boolean! 
}